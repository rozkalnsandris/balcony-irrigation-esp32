name: phase2-apply

on:
  push:
    branches:
      - firmware/bounded-mqtt-reconnect

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: echo phase2-patcher-ready
